---
"inquiso": patch
---

Give the landing page its own identity instead of a Gemini look-alike. Drop the
time-of-day greeting and the blue→cyan gradient; lead with the Inquiso mark and
a confident tagline, tidy one-tap suggestion rows, and the local-first promise
as a signature footer. The accent moves from Google-ish indigo/cyan to a single
Inquiso violet (retiring the gradient partner colour), which also refreshes
buttons, links, and the send control app-wide. Composer, suggestions, and
sending are unchanged.
