---
"inquiso": patch
---

Chat-app-grade UI polish across the side panel, modeled on the ChatGPT/
Gemini/Claude/Grok mobile conventions: light-dark() design tokens (surfaces,
ink, hairlines) for automatic theme parity; a shared animated Menu popover
replacing every native select and ad-hoc dropdown (tiered model picker,
history list with hover-reveal delete, "+" context menu); an auto-growing
pill composer with circular send/stop icon buttons; icon-button header;
user bubbles capped at 85% width with asymmetric corners; stick-to-bottom
transcript scrolling; and restyled confirm, error, and tool cards.
