---
"inquiso": patch
---

Add Inquiso's own logo — the "Query" mark: a ring + focus dot (an aperture that
reads the page) with a short tail that turns it into a Q, for query/inquiry.
Deliberately avoids the sparkle/orb/magnifier AI clichés, monoline in the brand
violet, legible to 16px. Added as a reusable Logo component (shown in the header
and the landing page) plus a standalone tile SVG (src/assets/logo.svg) for the
extension/store icon. The old sparkle glyph on the landing page is replaced by
the real mark.
