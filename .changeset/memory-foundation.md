---
"inquiso": minor
---

MemoryAgent foundation: append-only typed event ledger for every run, evidence-based outcome
verification for page-mutating tools (a dispatched click is no longer assumed to have worked),
and durable task checkpoints with cross-restart resume — interrupted tasks are detected and
resumed with explicit revalidation instructions instead of blind trust in stale state.
