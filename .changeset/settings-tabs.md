---
"inquiso": patch
---

Organise Settings into tabs — Models, Agent, Access, and MCP — instead of one
long scroll. Each concern lives on its own panel behind a segmented tab bar, so
provider/key management, run behaviour, permissions, and MCP servers are each
easy to find. New tab labels and the recent permission/MCP/voice strings are
added to the i18n catalog.
