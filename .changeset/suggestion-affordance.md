---
"inquiso": patch
---

Make the landing-page suggestions obviously clickable. They were borderless
rows that read as plain text until hover; now each is a bordered card with a
resting surface, a pointer cursor, and a trailing arrow — with a hover state
that darkens the border and nudges the arrow, so it's clear you can tap them.
