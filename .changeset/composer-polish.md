---
"inquiso": patch
---

Declutter the composer control row. The scope button ("This page") no longer
wraps to two lines, and the autonomy picker uses a compact one-word label
(Ask / Auto / Autopilot) instead of the long "Autopilot (confirm risky)" — the
full descriptions stay in Settings, and the selected level's full label is a
hover tooltip. Controls are flat and consistent, so the row reads cleanly.
