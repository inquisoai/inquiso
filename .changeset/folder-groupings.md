---
'inquiso': patch
---

Grouped sibling files that form one concept into folders (no behavior change): `core/messaging/schemas/`, `core/agent/loop/`, `core/providers/defs/`, `ui/components/menu/`, `ui/features/settings/{sync,providers}/`, `ui/features/chat/{composer,transcript}/`, and `tests/unit/` now mirrors the `src/` tree.
